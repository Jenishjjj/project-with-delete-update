<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .main{
            width: 100%;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .box{
            background-color: bisque;
            padding: 30px;
            border-radius: 20px;
        }
        
        td{
            border-bottom: 2px solid black;
            padding: 15px;
        }
        .btn{
            cursor: pointer;
            background-color: brown;
            color: white;
        }
    </style>
</head>
<body>
    <div class="main">
        <div class="box">
            <a href="/">Add Book Data</a>
        <table>
            <tr>
                <td>Book Title</td>
                <td>ISBN code</td>
                <td>Author name</td>
                <td>Publisher name</td>
                <td>Book quantity</td>
                <td>Total page</td>
                <td>Book type</td>
                <td>Rating</td>
                <td>Published Date</td>
                <td>Image</td>
                <td>Action</td>
            </tr>
            
            <% for(var book of bookData){ %>
                <tr>
                    <td><%= book.title %></td>
                    <td><%= book.isbn %></td>
                    <td><%= book.author %></td>
                    <td><%= book.publisher %></td>
                    <td><%= book.quantity %></td>
                    <td><%= book.total_page %></td>
                    <td><%= book.book_type %></td>
                    <td><%= book.rating %></td>
                    <td><%= book.published_date %></td>
                    <td><img src="<%=book.images %>" height="100"></td>
                    <td>
                        <a href="/deleteRec/<%= book.id %>">Delete</a> ||
                        <a href="/updateRec/?id=<%= book.id %>">Update</a>
                    </td>
                </tr>
            <% } %>

        </table>
        </div>
    </div>
</body>
</html>